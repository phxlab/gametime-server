{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "docker compose up -d && docker logs tests -f"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^3.6.0",
    "mongoose": "^7.5.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.3",
    "@types/supertest": "^2.0.12",
    "bun-types": "^0.6.2",
    "prettier": "^3.0.3",
    "supertest": "^6.3.3"
  }
}
